[$build-with-team](/Users/bjornjee/.agents/skills/build-with-team/SKILL.md) get inspiration from sales-car-inspection-demo. i want to a build a browser site which takes in a car scanning report, exterior and interior images at fixed locations, extend to videos with audio for startup engine, blinkers, headlights taken from a client. this app will be for a inspector persona. it will have a pipeline to scan and process the report to have an itemised list of parts which are defect with the inventory of prices - we will have a database for this, we can start with sqlite. it will be keyed by car brand, model, year etc. We can use toyota corolla altis for this only, with the correct abstrctions to extend to other parts. we have the haynes manual for this car to get the whole list of parts, but should only surface the import parts as the smaller ones will need a thorough inspection by a mechanic.  this list should be able to detect the smallest possible item to replace. for example if the bulb in the headlight is faulty, we just have to change the build, not the whole headlight. this itemised list should be able to add and remove by the inspector as he check the car manually. after this itemised list is submitted, the next flow will be to search a company inventory if the parts is readily available. if not available, it should search for potential suppliers to prepare a PO. in this, we can have the suppliers, inventory all fabricated in the sqlite db.&#x20;
the design should be similar to cars24 demo. also, the client is very particular on the accuracy of the defect identification and the severity. it should have a clean abstraction so that we can able to tune the classifier separately. keep this a simple design and architecture. we can just care about the application for now to be tested locally. it will be written in python BE with js FE.
